+ =======================================================================================
+ ESO Pipelines installation notes (EDPS + EsoRelfex - both use EsoRex)
+ see: https://www.eso.org/sci/software/pipe_aem_main.html
+ =======================================================================================

* Installation of a pipeline with EsoRelfex in macOS

https://www.eso.org/sci/software/pipelines/installation/macports.html

As a first case, I have installed the UVES pipeline (esopipe-uves-all)

? Note: '/opt/local/share/esopipes/workflows' is where the pipelines are located
? Note: The demo data can be found in '/opt/local/share/esopipes/datademo/uves'

The log from installing the esopipe-uves-all port is as follows:

...
-> Some of the ports you installed have notes:
  db48 has the following notes:
    The Java and Tcl bindings are now provided by the db48-java and
    db48-tcl subports.
  hdf5 has the following notes:
    Mac users may need to set the environment variable "HDF5_USE_FILE_LOCKING" to the
    five-character string "FALSE" when accessing network mounted files. This is an
    application run-time setting, not a configure or build setting. Otherwise errors such
    as "unable to open file" or "HDF5 error" may be encountered.
  lzma has the following notes:
    The LZMA SDK program is installed as "lzma_alone" to avoid conflict with LZMA Utils.
  openjdk11 has the following notes:
    If you want to make openjdk11 the default JDK, add this to shell profile:
    export JAVA_HOME=/Library/Java/JavaVirtualMachines/openjdk11/Contents/Home
  py311-fonttools has the following notes:
    To make the Python 3.11 version of fonttools the one that is run when you execute the
    commands without a version suffix, e.g. 'fonttools', run:

        sudo port select --set fonttools fonttools-311
  py311-matplotlib has the following notes:
    The default backend is the interactive Mac OS X backend. Different backends can be
    specified using the ~/.matplotlib/matplotlibrc file. More details regarding backends
    can be found in the matplotlib FAQ:

            https://matplotlib.org/stable/users/explain/backends.html
  python311 has the following notes:
    To make this the default Python or Python 3 (i.e., the version run by the 'python' or
    'python3' commands), run one or both of:

        sudo port select --set python python311
        sudo port select --set python3 python311
  python313 has the following notes:
    To make this the default Python or Python 3 (i.e., the version run by the 'python' or
    'python3' commands), run one or both of:

        sudo port select --set python python313
        sudo port select --set python3 python313
  tcl has the following notes:
    The Sqlite3 Tcl package is now being provided by the sqlite3-tcl port:
    sudo port install sqlite3-tcl
  xinit has the following notes:
    To use MacPorts' X11 as the default server, install xorg-server, log out, and log back
    in.
...

? Note: When running > esoreflex, I got a problem with the java version (21).
? See: https://support.eso.org/en-GB/kb/articles/known-issues-esoreflex-and-pipelines-on-macos#java_21

* Installation of EDPS

https://edps.readthedocs.io/en/latest/quick/installation.html

It requests to install graphviz, fv, and ds9.

- graphviz is installed via MacPorts as (below is the output log)

~ $sudo port install graphviz
  coreutils has the following notes:
    The tools provided by GNU coreutils are prefixed with the character 'g' by default to distinguish them from the BSD commands.
    For example, cp becomes gcp and ls becomes gls.

    If you want to use the GNU tools by default, add this directory to the front of your PATH environment variable:
        /opt/local/libexec/gnubin/
  libheif has the following notes:
    Support for rav1e now disabled by default; enable via +rav1e
  shared-mime-info has the following notes:
    A startup item has been generated that will start shared-mime-info with launchd, and will be enabled automatically on activation. Execute the following command to manually _disable_ it:

        sudo port unload shared-mime-info

! For fv and ds9, I have them installed, both via macOS disk image (app) but both should
! I think I must install them via Source to be able to include them in the system path.
! For DS9 I get compilation errors so I am skipping it for now...

I have then created a virtual environment for EDPS in:
~ $ python3 -m venv ~/Documents/pipelines/

? Note 1: I had a problem in which EDPS was looking an old path for reduced files.
? I had to change it with $ open -a textedit ~/.edps/application.properties
? In particular: base_dir= and path=

? Note 2: For UVES pipeline, I had to replace uves_datasources.py file located in
? /opt/local/share/esopipes/workflows/uves-6.5.3/uves by the one that Lodovico
? provided me (see "UVES data reduction problem" email). This extends the range
? of dates where to look for the files of the standard star.

? Note 3: For UVES pipeline, I had to modify the uves_parameters.yaml file replacing
? response: "master" by response: "night", so that the pipeline uses the STD to produce
? the response function and ultimately the flux calibrated spectra.

* Usage of EDPS

Load the virtual environment:
~ $ cd ~/Documents/pipelines/
~ $ . edpsgui/bin/activate
(edpsgui) ~ $ edps-gui

To deactivate the environment:
~ $ deactivate

UVES configuration files for EDPS are located in:
/opt/local/share/esopipes/workflows/uves-6.5.3/uves/